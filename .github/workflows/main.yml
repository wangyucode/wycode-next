name: Frontend

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Node.js environment
        uses: actions/setup-node@v2.1.5
        with: 
          node-version: '20'
      - name: Cache node modules
        uses: actions/cache@v2
        env:
          cache-name: cache-node-modules
        with:
          # npm cache files are stored in `~/.npm` on Linux/macOS
          path: ~/.npm
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.os }}-build-
            ${{ runner.os }}-

      - name: install
        run: npm ci

      - name: build
        run: npm run build
      
      - name: export
        run: npm run export
      
      - name: Get the version from package.json
        run: |
          echo "VERSION=$(jq -r .version package.json)" >> "$GITHUB_ENV"
          echo "VERSION=${{ env.VERSION }}"

      - name: Tar files in out directory
        run: |
          echo "VERSION=${{ env.VERSION }}"
          tar -czvf wycode-${{ env.VERSION }}.tar.gz -C out/ .
      
      - name: upload to tencent COS
        uses: zkqiang/tencent-cos-action@v0.1.0
        with:
          args: delete -r -f release/ && upload -r wycode-${{ env.VERSION }}.tar.gz release/
          secret_id: ${{ secrets.TENCENT_CLOUD_SECRET_ID }}
          secret_key: ${{ secrets.TENCENT_CLOUD_SECRET_KEY }}
          bucket: ${{ secrets.TENCENT_COS_BUCKET }}
          region: ap-beijing

      
      # - name: Call Crane to Deploy
      #   run: |
      #     curl -H "X-Api-Key: ${{secrets.CRANE_API_KEY}}" https://wycode.cn/crane/deploy?url=https://github.com/wangyucode/wycode-next/releases/download/2.1.0/wycode-2.1.0.tar.gz

      # - name: Deploy to Github pages
      #   uses: crazy-max/ghaction-github-pages@v2.1.3
      #   with:
      #     repo: wangyucode/wangyucode.github.io
      #     target_branch: master
      #     build_dir: out
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.REPO_PUSH_TOKEN }}

      # - name: Send posts to algolia
      #   run: npm run algolia ${{ secrets.ALGOLIA_KEY }}

      # - name: Send Email
      #   run: |
      #     curl -m 10 -i -X POST \
      #       -H "Content-Type:application/json" \
      #       -d \
      #       '{"key": "${{ secrets.MAIL_TOKEN }}","subject":"Frontend Build","content":"frontend deployed","to": "wangyu@wycode.cn"}' \
      #       'https://wycode.cn/api/v1/email'
      # - name: Notify Google
      #   run: curl -m 10 http://www.google.com/webmasters/sitemaps/ping?sitemap=https://wycode.cn/sitemap.xml